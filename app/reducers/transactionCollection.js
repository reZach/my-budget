import {
    Action,
    update
} from "./types";

export default function transactionCollection(state: any = {}, action: Action){
    switch (action.type){
        default:
            return state;
    }
}